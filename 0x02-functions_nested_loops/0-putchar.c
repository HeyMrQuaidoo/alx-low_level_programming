#include <stdio.h>

/**
 *  main - Prints _putchar, followed by new line
 *
 *  Return: Always 0 (Success)
 *
 */

int main(void)
{
	char n = '_';
	char p = 'p';
	char u = 'u';
	char t = 't';
	char c = 'c';
	char h = 'h';
	char a = 'a';
	char r = 'r';

	putchar(n);
	putchar(p);
	putchar(u);
	putchar(t);
	putchar(c);
	putchar(h);
	putchar(a);
	putchar(r);
	putchar('\n');
	return (0);
}
