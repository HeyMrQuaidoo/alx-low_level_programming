#ifndef FILE_MAIN
#define FILE_MAIN
#include <stddef.h>

int _putchar(char c);
void _puts_recursion(char *s);
int _strlen_recursion(char *s);
int factorial(int n);
int _pow_recursion(int x, int y);
#endif
